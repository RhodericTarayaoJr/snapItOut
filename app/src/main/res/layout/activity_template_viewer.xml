<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootTemplateViewer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5EDE0"
    tools:context=".TemplateViewerActivity">

    <!-- Back Button -->
    <ImageButton
        android:id="@+id/btnBack"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Back"
        android:rotationY="180"
        android:scaleType="fitCenter"
        android:src="@drawable/arrow2"
        app:layout_constraintBottom_toBottomOf="@+id/imgDecor"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/imgDecor" />

    <!-- Decor Icon -->
    <ImageView
        android:id="@+id/imgDecor"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="Decor"
        android:src="@drawable/frames"
        app:layout_constraintBottom_toBottomOf="@+id/templateNameView"
        app:layout_constraintEnd_toStartOf="@+id/templateNameView"
        app:layout_constraintStart_toEndOf="@id/btnBack"
        app:layout_constraintTop_toTopOf="@+id/templateNameView" />

    <!-- Title -->
    <TextView
        android:id="@+id/templateNameView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="16dp"
        android:fontFamily="@font/lora"
        android:gravity="center"
        android:text="Template Name"
        android:textColor="#000000"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/imgDecor"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- ScrollView + Center Wrapper + Grid -->
    <ScrollView
        android:id="@+id/scrollArea"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/templateNameView"
        app:layout_constraintBottom_toTopOf="@+id/buttonContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- CENTER CONTAINER -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingTop="12dp"
            android:paddingBottom="12dp">

            <!-- CENTERED GRID -->
            <GridLayout
                android:id="@+id/gridViewer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:alignmentMode="alignBounds"
                android:columnCount="2"
                android:padding="12dp" />

        </LinearLayout>
    </ScrollView>

    <!-- Buttons Container -->
    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Edit Template Button -->
        <Button
            android:id="@+id/btnEditTemplate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="#E9D0BC"
            android:fontFamily="@font/lora"
            android:paddingHorizontal="24dp"
            android:paddingVertical="10dp"
            android:text="Edit Template"
            android:textColor="@color/black"
            android:textSize="16sp" />

        <!-- Use Template Button -->
        <Button
            android:id="@+id/btnUseTemplate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:backgroundTint="#E9D0BC"
            android:fontFamily="@font/lora"
            android:paddingHorizontal="24dp"
            android:paddingVertical="10dp"
            android:text="Use Template"
            android:textColor="@color/black"
            android:textSize="16sp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
